find_package(Threads REQUIRED)
add_library(vita_benchmark_allocation SHARED allocation.cpp)
target_compile_features(vita_benchmark_allocation PRIVATE cxx_std_23)
add_executable(vita_benchmark main.cpp)
target_link_libraries(vita_benchmark PRIVATE vita::posix_udp Threads::Threads vita_benchmark_allocation)
target_compile_options(vita_benchmark PRIVATE -Wall -Wextra -Wpedantic -fno-exceptions -fno-rtti)
if(BUILD_TESTING)
  vita_add_test(p13_allocation_probe ${PROJECT_SOURCE_DIR}/tests/unit/P13/allocation_probe.cpp)
  target_include_directories(p13_allocation_probe PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})
  target_link_libraries(p13_allocation_probe PRIVATE vita_benchmark_allocation)
  if(NOT APPLE)
    set_tests_properties(p13_allocation_probe PROPERTIES DISABLED TRUE)
  endif()
  add_test(NAME p13_runner_smoke COMMAND vita_benchmark --duration-seconds 0.2 --warmup-seconds 0 --burst-every-seconds 0.1 --output-dir ${PROJECT_BINARY_DIR}/p13-smoke)
  set_tests_properties(p13_runner_smoke PROPERTIES LABELS "benchmark;functional" TIMEOUT 15)
endif()
foreach(program receiver_benchmark replay_sender)
  if(program STREQUAL "receiver_benchmark")
    set(source receiver_main.cpp)
  else()
    set(source replay_main.cpp)
  endif()
  add_executable(vita_${program} ${source})
  target_link_libraries(vita_${program} PRIVATE vita::posix_udp Threads::Threads vita_benchmark_allocation)
  target_compile_options(vita_${program} PRIVATE -Wall -Wextra -Wpedantic -fno-exceptions -fno-rtti)
endforeach()
