vita_add_test(p12_verify_factory factory_contract.cpp)
vita_add_test(p12_verify_framing framing_contract.cpp)
if(TARGET vita::posix_udp)
  foreach(pair IN ITEMS "socket;socket_contract.cpp" "receive;receive_contract.cpp" "isolation;isolation_contract.cpp" "mtu;mtu_contract.cpp" "rx_isolation;rx_isolation.cpp")
    list(GET pair 0 name)
    list(GET pair 1 source)
    vita_add_test(p12_verify_${name} ${source})
    target_link_libraries(p12_verify_${name} PRIVATE vita::posix_udp)
  endforeach()
endif()
if(TARGET vita::posix_udp)
  vita_add_test(p12_verify_syscall syscall_contract.cpp)
  target_link_libraries(p12_verify_syscall PRIVATE vita::posix_udp)
endif()
if(TARGET vita::posix_udp)
  vita_add_test(p12_verify_cycle cycle_contract.cpp)
  target_link_libraries(p12_verify_cycle PRIVATE vita::posix_udp)
endif()
if(TARGET vita::posix_udp)
  vita_add_test(p12_verify_runtime runtime_contract.cpp)
  target_link_libraries(p12_verify_runtime PRIVATE vita::posix_udp)
endif()
if(TARGET vita::posix_udp)
  vita_add_test(p12_verify_allocation allocation_contract.cpp)
  target_link_libraries(p12_verify_allocation PRIVATE vita::posix_udp)
endif()
if(TARGET vita::posix_udp)
  vita_add_test(p12_verify_callback callback_contract.cpp)
  target_link_libraries(p12_verify_callback PRIVATE vita::posix_udp)
endif()
