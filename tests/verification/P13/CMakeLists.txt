vita_add_test(p13_verify_trace trace_contract.cpp)
vita_add_test(p13_verify_runtime_trace runtime_trace.cpp)
if(TARGET vita::posix_udp)
  vita_add_test(p13_verify_ingress ingress_contract.cpp)
  target_link_libraries(p13_verify_ingress PRIVATE vita::posix_udp)
endif()
vita_add_test(p13_verify_ring ring_contract.cpp)
if(APPLE AND TARGET vita_benchmark_allocation)
  vita_add_test(p13_verify_allocation_probe allocation_probe.cpp)
  target_link_libraries(p13_verify_allocation_probe PRIVATE vita_benchmark_allocation)
endif()

add_test(NAME p13_verify_oracle COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/oracle_contract.py)

vita_add_test(p13_verify_retention_index retention_index_contract.cpp)
vita_add_test(p13_verify_pressure pressure_contract.cpp)
if(TARGET vita::posix_udp)
  vita_add_test(p13_verify_late_capture late_capture_contract.cpp)
  target_link_libraries(p13_verify_late_capture PRIVATE vita::posix_udp)
endif()

if(UNIX)
  vita_add_test(p13_verify_payload payload_contract.cpp)
endif()
vita_add_test(p13_verify_payload_runtime payload_runtime_contract.cpp)

if(TARGET vita::posix_udp)
  vita_add_test(p13_verify_receiver_wire receiver_wire_contract.cpp)
  target_link_libraries(p13_verify_receiver_wire PRIVATE vita::posix_udp)
endif()
if(TARGET vita::posix_udp)
  vita_add_test(p13_verify_receiver_core receiver_core_contract.cpp)
  target_link_libraries(p13_verify_receiver_core PRIVATE vita::posix_udp)
endif()
add_test(NAME p13_verify_receiver_analysis COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/receiver_analysis_contract.py)
