vita_add_test(p16_verify_wire wire_contract.cpp)
set_tests_properties(p16_verify_wire PROPERTIES LABELS "codec;profile;oracle")
vita_add_test(p16_verify_scene scene_contract.cpp)
set_tests_properties(p16_verify_scene PROPERTIES LABELS "profile;source;oracle")
vita_add_test(p16_verify_kernel kernel_contract.cpp)
set_tests_properties(p16_verify_kernel PROPERTIES LABELS "profile;transaction;oracle")
vita_add_test(p16_verify_sweep sweep_contract.cpp)
set_tests_properties(p16_verify_sweep PROPERTIES LABELS "profile;cli;oracle")
vita_add_test(p16_verify_public public_contract.cpp)
set_tests_properties(p16_verify_public PROPERTIES LABELS "profile;lifetime;oracle")
vita_add_test(p16_verify_budget budget_contract.cpp)
set_tests_properties(p16_verify_budget PROPERTIES LABELS "profile;budget;oracle")
if(TARGET vita_posix_udp)
  vita_add_test(p16_verify_remote remote_contract.cpp)
  target_link_libraries(p16_verify_remote PRIVATE vita::posix_udp)
  set_tests_properties(p16_verify_remote PROPERTIES LABELS "profile;udp;oracle")
endif()
vita_add_test(p16_verify_deferred deferred_contract.cpp)
set_tests_properties(p16_verify_deferred PROPERTIES LABELS "profile;completion;oracle")
vita_add_test(p16_verify_counters counter_contract.cpp)
set_tests_properties(p16_verify_counters PROPERTIES LABELS "profile;transport;lifetime;oracle")
vita_add_test(p16_verify_scene_runtime scene_runtime_contract.cpp)
set_tests_properties(p16_verify_scene_runtime PROPERTIES LABELS "profile;source;transaction;oracle")
if(APPLE AND TARGET vita_benchmark_allocation)
  vita_add_test(p16_verify_scene_hot_alloc scene_runtime_contract.cpp)
  target_compile_definitions(p16_verify_scene_hot_alloc PRIVATE P16_ALLOCATION_GUARD=1)
  target_link_libraries(p16_verify_scene_hot_alloc PRIVATE vita_benchmark_allocation)
  set_tests_properties(p16_verify_scene_hot_alloc PROPERTIES LABELS "profile;allocation;oracle")
endif()
vita_add_test(p16_verify_endpoint endpoint_contract.cpp)
set_tests_properties(p16_verify_endpoint PROPERTIES LABELS "profile;cli;oracle")
vita_add_test(p16_verify_rf_lifecycle rf_lifecycle_contract.cpp)
set_tests_properties(p16_verify_rf_lifecycle PROPERTIES LABELS "profile;lifecycle;cancellation;oracle")
vita_add_test(p16_verify_owned_budget owned_budget_contract.cpp)
set_tests_properties(p16_verify_owned_budget PROPERTIES LABELS "profile;budget;ownership;oracle")
if(NOT DEFINED VITA_BUILD_EXAMPLES OR VITA_BUILD_EXAMPLES)
  add_test(NAME p16_verify_process_combined COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/process_contract.py" --mode combined --combined $<TARGET_FILE:vita_frequency_scan_combined> --output-dir "${CMAKE_CURRENT_BINARY_DIR}/process-combined")
  set_tests_properties(p16_verify_process_combined PROPERTIES LABELS "profile;examples;integration;oracle" TIMEOUT 20)
  if(TARGET vita::posix_udp)
    foreach(mode remote timeout)
      add_test(NAME p16_verify_process_${mode} COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_SOURCE_DIR}/process_contract.py" --mode ${mode} --controller $<TARGET_FILE:vita_frequency_scan_controller> --controllee $<TARGET_FILE:vita_frequency_scan_controllee> --output-dir "${CMAKE_CURRENT_BINARY_DIR}/process-${mode}")
      set_tests_properties(p16_verify_process_${mode} PROPERTIES LABELS "profile;examples;udp;oracle" TIMEOUT 20)
    endforeach()
  endif()
endif()
vita_add_test(p16_verify_validation_rejection rejection_contract.cpp)
set_tests_properties(p16_verify_validation_rejection PROPERTIES LABELS "profile;examples;ack;oracle")
vita_add_test(p16_verify_validation validation_contract.cpp)
set_tests_properties(p16_verify_validation PROPERTIES LABELS "profile;ack;oracle")
if(NOT DEFINED VITA_BUILD_EXAMPLES OR VITA_BUILD_EXAMPLES)
  if(TARGET vita::posix_udp)
    set_tests_properties(p16_verify_process_remote p16_verify_process_timeout PROPERTIES RESOURCE_LOCK p16_process_ports)
  endif()
endif()
