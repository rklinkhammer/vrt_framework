add_executable(vita_fuzz_replay replay.cpp)
target_link_libraries(vita_fuzz_replay PRIVATE vita::core)
target_compile_options(vita_fuzz_replay PRIVATE -fno-exceptions)
if(BUILD_TESTING)
  add_test(NAME malformed_packet_mutation_replay COMMAND vita_fuzz_replay ${CMAKE_CURRENT_SOURCE_DIR}/corpus 100000 0x564954413439)
  set_tests_properties(malformed_packet_mutation_replay PROPERTIES LABELS "fuzz;codec" TIMEOUT 60)
endif()
option(VITA_ENABLE_LIBFUZZER "Build coverage-guided packet fuzzer with Clang libFuzzer" OFF)
if(VITA_ENABLE_LIBFUZZER)
  if(NOT CMAKE_CXX_COMPILER_ID MATCHES "Clang" OR VITA_SANITIZER STREQUAL "thread")
    message(FATAL_ERROR "libFuzzer requires Clang and cannot use the thread-sanitizer preset")
  endif()
  add_executable(vita_packet_fuzzer libfuzzer.cpp)
  target_link_libraries(vita_packet_fuzzer PRIVATE vita::core)
  target_compile_options(vita_packet_fuzzer PRIVATE -fsanitize=fuzzer,address,undefined -fno-exceptions)
  target_link_options(vita_packet_fuzzer PRIVATE -fsanitize=fuzzer,address,undefined)
endif()
if(BUILD_TESTING)
  add_executable(vita_fuzz_capacity capacity.cpp)
  target_link_libraries(vita_fuzz_capacity PRIVATE vita::core)
  target_compile_options(vita_fuzz_capacity PRIVATE -fno-exceptions)
  add_test(NAME malformed_packet_capacity_contract COMMAND vita_fuzz_capacity)
  set_tests_properties(malformed_packet_capacity_contract PROPERTIES LABELS "fuzz;codec" TIMEOUT 30)
endif()
